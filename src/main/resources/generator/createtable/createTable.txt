<changeSet author="$AUTHOR$" id="$ID$" dbms="mssql">
	<preConditions onFail="MARK_RAN">
		<not>
			<tableExists tableName="$TABLE_NAME$" />
		</not>
	</preConditions>

	<comment>Create table $TABLE_NAME$</comment>
	<createTable tableName="$TABLE_NAME$">

		<column name="$COLUMN_PK_NAME$" type="integer" autoIncrement="true">
			<constraints primaryKeyName="$CONSTRAIN_PK_NAME$" primaryKey="true" />
		</column>

	</createTable>
</changeSet>

<changeSet author="$AUTHOR$" id="$ID$" dbms="oracle, postgresql">
	<preConditions onFail="MARK_RAN">
		<not>
			<tableExists tableName="$TABLE_NAME$" />
		</not>
	</preConditions>

	<comment>Create table $TABLE_NAME$</comment>
	<createTable tableName="$TABLE_NAME$">

		<column name="$COLUMN_PK_NAME$" type="integer">
			<constraints primaryKeyName="$CONSTRAIN_PK_NAME$" primaryKey="true" />
		</column>

	</createTable>
</changeSet>

<changeSet author="$AUTHOR$" id="$ID$" dbms="oracle">
	<preConditions onFail="MARK_RAN">
		<not>
			<sequenceExists sequenceName="$SEQUENCE_NAME$" />
		</not>
	</preConditions>
	<comment>Create sequence $SEQUENCE_NAME$ on Oracle if it doesn't exist.</comment>
	<createSequence sequenceName="$SEQUENCE_NAME$" startValue="50"
		incrementBy="50" cacheSize="20" maxValue="999999999999999999999999999" ordered="false"
		cycle="false" />
</changeSet>

<changeSet author="$AUTHOR$" id="$ID$" dbms="postgresql">
	<preConditions onFail="MARK_RAN">
		<not>
			<sequenceExists sequenceName="$SEQUENCE_NAME$" />
		</not>
	</preConditions>
	<comment>Create sequence $SEQUENCE_NAME$ on postgreSQL if it doesn't exist.</comment>
	<createSequence sequenceName="SEQ_NDER_DATAELEMENT" startValue="50"
		incrementBy="50" cacheSize="20" maxValue="999999999999999999" cycle="false" />
</changeSet>
